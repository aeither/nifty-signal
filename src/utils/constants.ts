import { PublicKey } from '@solana/web3.js'


// if (!process.env.PROGRAM_ID) throw "PROGRAM_ID not found";
// const _PROGRAM_ID = new Error('PROGRAM_ID not found')

/** Solana */
// export const PROGRAM_ID = new PublicKey(_PROGRAM_ID || '')
// export const TW_COLLECTION_ADDRESS =
//   process.env.VITE_TW_COLLECTION_ADDRESS || ''
// export const NETWORK_URL = process.env.VITE_RPC_URL || ''
